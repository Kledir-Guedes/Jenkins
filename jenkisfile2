pipeline {
    agent any
    environment {
        DISABLE_AUTH = 'true'
        DB_ENGINE    ='.'
    }
    stages {
        stage('Example Username/Password1') {
           environment {
               DB_ENGINE='teste'
               }
           steps {
               
               echo "Database engine is ${DB_ENGINE}"
               echo "DISABLE_AUTH is ${DISABLE_AUTH}"
            }
        }
     }
}
